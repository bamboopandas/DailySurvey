[project]
name = "auto-search"
version = "0.1.0"
description = "Daily AI and recommender-system intelligence brief for Codex automations."
requires-python = ">=3.9"

[tool.unittest]
start-dir = "tests"

